{
  "$schema": "https://raw.githubusercontent.com/jetify-com/devbox/0.17.3/.schema/devbox.schema.json",
  "packages": [
    "go@1.26",
    "kubectl@latest",
    "kubernetes-helm@latest",
    "jq@latest",
    "cosign@latest",
    "nodejs@24",
    "go-task@latest"
  ],
  "env": {
    "DEVBOX_COREPACK_ENABLED": "1"
  },
  "shell": {
    "init_hook": [
      "echo 'buildkit-operator devbox ready: go-task, go, node+pnpm (corepack), kubectl, helm, jq, cosign. Run tasks with: devbox run -- task <name> (task --list). controller-gen & golangci-lint stay pinned in the Taskfile.'"
    ],
    "scripts": {
      "test":      ["task test"],
      "lint":      ["task lint"],
      "manifests": ["task manifests"],
      "build":     ["task build"]
    }
  }
}
